import { expect } from 'chai'

describe('Resource', () => {
  describe('create', () => {
    xit('needs tests')
  })

  describe('parse', () => {
    xit('needs tests')
  })

  describe('validate', () => {
    xit('needs tests')
  })

  describe('verify', () => {
    xit('needs tests')
  })

  describe('sign', () => {
    xit('needs tests')
  })
})